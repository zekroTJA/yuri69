import { RouteContainer } from '../components/RouteContainer';

type Props = {};

export const UploadRoute: React.FC<Props> = ({}) => {
  return (
    <RouteContainer>
      <h1>Upload</h1>
    </RouteContainer>
  );
};

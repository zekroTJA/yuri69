import { RouteContainer } from '../components/RouteContainer';

type Props = {};

export const SettingsRoute: React.FC<Props> = ({}) => {
  return (
    <RouteContainer>
      <h1>Settings</h1>
    </RouteContainer>
  );
};
